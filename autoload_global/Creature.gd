extends Node

enum IdleState {
	IDLE,
	WORKING,
	PLAYING,
}

enum Skill {
	HAULING,
	FARMING,
	FIGHTING,
	BUILDING
}

enum Type {
	TYPE_HUMANOID,
	TYPE_BEAST,
}

enum Subtype {
	SUBTYPE_ORC,
	SUBTYPE_DWARF,
}
